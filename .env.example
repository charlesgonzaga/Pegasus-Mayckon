# ==============================================================================
# Variáveis de Ambiente - Pegasus (Sistema de Gestão NFSe e CT-e)
# Copie este arquivo para .env e preencha os valores
# ==============================================================================

# --- Banco de Dados (MySQL) ---
# ⚠️  ATENÇÃO: o host DEVE ser "db" (nome do serviço Docker Compose)
# ⚠️  NUNCA use "localhost" — dentro do container, localhost aponta para o próprio app, não para o MySQL!
# DATABASE_URL: mysql://usuario:senha@HOST:porta/banco  →  HOST = db (não localhost!)
# MYSQL_ROOT_PASSWORD: Senha do superusuário root do MySQL.
# MYSQL_DATABASE: Nome do banco criado automaticamente pelo Docker.
# MYSQL_USER: Usuário da aplicação (criado automaticamente pelo Docker).
# MYSQL_PASSWORD: Senha do usuário da aplicação.
DATABASE_URL=mysql://pegasus:SuaSenhaForte123@db:3306/pegasus
MYSQL_ROOT_PASSWORD=SenhaRootForte456
MYSQL_DATABASE=pegasus
MYSQL_USER=pegasus
MYSQL_PASSWORD=SuaSenhaForte123



# --- Aplicação ---
NODE_ENV=production
PORT=3000

# --- Autenticação JWT ---
# Gere com: openssl rand -hex 32
JWT_SECRET=troque-por-uma-chave-secreta-forte-e-unica-com-64-caracteres
